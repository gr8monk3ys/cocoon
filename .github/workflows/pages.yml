name: Pages

on:
  push:
    branches: ["main"]
    paths:
      - "docs/**"
      - ".github/workflows/pages.yml"
  # Manual redeploy. Pages can fail for reasons unrelated to a docs change
  # (cert provisioning, custom-domain/DNS changes), and without this the only
  # way to retry is an empty commit under docs/.
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1

      - name: Setup Pages
        uses: actions/configure-pages@v6
        with:
          # Auto-enable Pages (GitHub Actions build type) so the first run does
          # not fail with "Get Pages site failed / Not Found".
          enablement: true

      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./docs

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
