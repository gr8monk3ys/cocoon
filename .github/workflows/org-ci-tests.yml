name: org-ci-tests

on:
  workflow_dispatch:
  schedule:
    - cron: "23 9 17 * *"  # monthly (minute-safe policy)

concurrency:
  group: org-ci-tests-${{ github.ref }}
  cancel-in-progress: true

permissions: read-all

jobs:
  ci-tests:
    uses: gr8monk3ys/github/.github/workflows/reusable-ci-tests-minimum.yml@4a306a94d72d10b8d7835d697521a469fc6deab1
